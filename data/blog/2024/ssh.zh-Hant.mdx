---
title: SSH的基本原理和用法
summary: SSH is a network protocol that enables secure login between two computers.
date: 2025-01-14T00:00:00.000Z
tags:
  - others
draft: false
images:
  - /static/images/blog/2024/ssh/Pasted%20image%2020250425193204.webp
authors:
  - default
---

- 前置知識：[數字簽名是什麼？ - 阮一峰的網絡日誌](https://www.ruanyifeng.com/blog/2011/08/what_is_a_digital_signature.html)
- 本文完全來源於：[SSH原理與運用（一）：遠程登錄 - 阮一峰的網絡日誌](https://www.ruanyifeng.com/blog/2011/12/ssh_remote_login.html)

簡單說，SSH是一種網絡協議，用於計算機之間的加密登錄。

如果一個用戶從本地計算機，使用SSH協議登錄另一臺遠程計算機，我們就可以認為，這種登錄是安全的，即使被中途截獲，密碼也不會洩露。

- 需要指出的是，SSH只是一種協議，存在多種實現，既有商業實現，也有開源實現。本文針對的實現是[OpenSSH](http://www.openssh.com/)，它是自由軟件，應用非常廣泛。

---

## 公鑰加密

SSH之所以能夠保證安全，原因在於它採用了**公鑰加密**。

整個過程是這樣的：

1. 遠程主機收到用戶的登錄請求，把自己的公鑰發給用戶。
2. 用戶使用這個公鑰，將登錄密碼加密後，發送回來。
3. 遠程主機用自己的私鑰，解密登錄密碼，如果密碼正確，就同意用戶登錄。

---

## 中間人攻擊

這個過程本身是安全的，但是實施的時候存在一個風險：如果有人截獲了登錄請求，然後冒充遠程主機，將偽造的公鑰發給用戶，那麼用戶很難辨別真偽。因為不像https協議，SSH協議的公鑰是沒有證書中心（CA）公證的，也就是說，都是自己簽發的。

可以設想，如果攻擊者插在用戶與遠程主機之間（比如在公共的wifi區域），用偽造的公鑰，獲取用戶的登錄密碼。再用這個密碼登錄遠程主機，那麼SSH的安全機制就蕩然無存了。這種風險就是著名的["中間人攻擊"](https://en.wikipedia.org/wiki/Man-in-the-middle_attack)（Man-in-the-middle attack）。

SSH協議是如何應對的呢？

---

## 口令登錄

如果你是第一次登錄對方主機，系統會出現下面的提示：

> $ ssh user@host
>
> The authenticity of host 'host (12.18.429.21)' can't be established.
>
> RSA key fingerprint is 98:2e:d7:e0:de:9f:ac:67:28:c2:42:2d:37:16:58:4d.
>
> Are you sure you want to continue connecting (yes/no)?

這段話的意思是，無法確認host主機的真實性，只知道它的公鑰指紋，問你還想繼續連接嗎？

所謂"公鑰指紋"，是指公鑰長度較長（這裡採用RSA算法，長達1024位），很難比對，所以對其進行MD5計算，將它變成一個128位的指紋。上例中是98:2e:d7:e0:de:9f:ac:67:28:c2:42:2d:37:16:58:4d，再進行比較，就容易多了。

> 很自然的一個問題就是，用戶怎麼知道遠程主機的公鑰指紋應該是多少？
> 回答是沒有好辦法，遠程主機必須在自己的網站上貼出公鑰指紋，以便用戶自行核對。

假定經過風險衡量以後，用戶決定接受這個遠程主機的公鑰。

```bash
Are you sure you want to continue connecting (yes/no)? yes
```

系統會出現一句提示，表示host主機已經得到認可。

```bash
Warning: Permanently added 'host,12.18.429.21' (RSA) to the list of known hosts.
```

然後，會要求輸入密碼。如果密碼正確，就可以登錄了。

當遠程主機的公鑰被接受以後，它就會被保存在文件`$HOME/.ssh/known_hosts`之中。下次再連接這臺主機，系統就會認出它的公鑰已經保存在本地了，從而跳過警告部分，直接提示輸入密碼。

每個SSH用戶都有自己的known_hosts文件，此外系統也有一個這樣的文件，通常是/etc/ssh/ssh_known_hosts，保存一些對所有用戶都可信賴的遠程主機的公鑰。

---

## 公鑰登錄

使用密碼登錄，每次都必須輸入密碼，非常麻煩。好在SSH還提供了公鑰登錄，可以省去輸入密碼的步驟。

所謂"公鑰登錄"，原理很簡單，就是**用戶將自己的公鑰儲存在遠程主機**上。

登錄的時候，遠程主機會向用戶發送一段隨機字符串，用戶用自己的**私鑰**加密後，再發回來。

遠程主機用事先儲存的**公鑰**進行解密，如果成功，就證明用戶是可信的，直接允許登錄shell，不再要求密碼。

這種方法要求用戶必須提供自己的公鑰。如果沒有現成的，可以直接用ssh-keygen生成一個：

```bash
ssh-keygen
```

運行上面的命令以後，系統會出現一系列提示，可以一路回車。其中有一個問題是，要不要對私鑰設置口令（passphrase），如果擔心私鑰的安全，這裡可以設置一個。

運行結束以後，在$HOME/.ssh/目錄下，會新生成兩個文件：id_rsa.pub和id_rsa。前者是你的公鑰，後者是你的私鑰。

---

## 雲服務器密鑰對

- 以阿里雲服務器為例

![](/static/images/blog/2024/ssh/Pasted%20image%2020250425193204.webp)

創建密鑰對之後網站會自動下載一個私鑰（pem文件），而阿里雲則持有一個公鑰。

1. 我們需要把私鑰文件添加到本地的 `~/.ssh/` 文件夾。
2. 配置 `~/.ssh/config` 如下：

```
Host <alias>
    HostName <server-ip>
    User <username>
    IdentityFile ~/.ssh/私钥文件.pem
```

- username默認root

之後即可直接 `ssh 别名` 登錄雲服務器。

---

## 本地密鑰對

和雲服務器密鑰對原理都是一樣的，只不過這次改用本地生成公私鑰，公鑰交給服務器。

但是這種情況是不安全的，一般用在攻擊服務器，給遠程寫入 ssh 密鑰文件，方便本地機器攻擊。

---

## ssh端口轉發

使得可以在本地瀏覽器訪問服務器的localhost

```bash
ssh -L 1080:127.0.0.1:8501 user@ipaddress

# -f 认证成功后转入后台运行 -N 仅作端口转发
ssh -fN -L 8000:127.0.0.1:8000 user@remote-server

# kill
ps aux | grep 'ssh.*127.0.0.1:8000'
```

效果是：

- 在本地訪問 `localhost:1080` 時，實際會訪問到服務器的 `127.0.0.1:8501`
